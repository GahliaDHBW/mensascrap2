(ns mensascrap2.core
  (:require [clojure.string :as str]
            [clj-http.client :as client]
            [cheshire.core :as cheshire]
            [hickory.core :as h]
            [hickory.select :as s])
  (:gen-class))

(inc 1)

(defn -main [& args]
  (println "Hello World"))
